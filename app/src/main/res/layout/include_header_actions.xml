<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="end|center_vertical"
    android:orientation="horizontal">

    <ImageButton
        android:id="@+id/button_header_theme"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@drawable/pay_header_icon_background"
        android:backgroundTint="@color/header_action_bg_default"
        android:contentDescription="@string/accessibility_toggle_theme"
        android:focusable="true"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_moon"
        android:tint="@color/header_action_icon_inactive" />

    <ImageButton
        android:id="@+id/button_header_notifications"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/pay_header_icon_background"
        android:backgroundTint="@color/header_action_bg_default"
        android:contentDescription="@string/accessibility_notifications"
        android:focusable="true"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_notifications"
        android:tint="@color/header_action_icon_inactive" />

    <ImageButton
        android:id="@+id/button_header_profile"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/pay_header_icon_background"
        android:backgroundTint="@color/header_action_bg_default"
        android:contentDescription="@string/accessibility_profile"
        android:focusable="true"
        android:padding="8dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_person"
        android:tint="@android:color/white" />

</LinearLayout>
